This is the application stack of the `click-count` project.

# How to setup ?

## Creation

This stack should be applied last, after `tf-net` and `tf-app`.

## Destruction

This stack should be destroyed first, before `tf-db` and before `tf-shared`.


